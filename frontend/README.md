## Frontend Workspace

This folder contains user-facing frontend apps for the SilverVisit demo:

- `sandbox-portal` for the mock appointment dashboard
- `extension` for the Chrome extension side panel flow

The frontend talks to the separately developed backend in `../backend`.
